# Hugging Face Token (required for PyAnnote models)
# Get token from: https://huggingface.co/settings/tokens
# Need access to: pyannote/speaker-diarization-3.1
HF_TOKEN=CHANGEME

# LLM API Configuration (required for summarization)
LLM_API_URL=http://localhost:11434/v1/chat/completions
LLM_MODEL_NAME=qwen2.5-32b-instruct
LLM_API_KEY=  # Optional, leave empty if not required

# PostgreSQL Database Configuration
POSTGRES_PASSWORD=  # Generate with: openssl rand -base64 32
DATABASE_URL=postgresql://meetmemo:${POSTGRES_PASSWORD}@postgres:5432/meetmemo

# NVIDIA GPU Configuration (optional)
NVIDIA_VISIBLE_DEVICES=all
